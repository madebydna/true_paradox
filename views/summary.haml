.article
  - haml_tag heading do
    %a{ :href => article.abspath }= article.heading
  - if article.summary.nil? || article.summary.empty?
    ~ article.body
  - else
    ~ article.summary
    %p.read_more
      Posted on
      = succeed '.' do
        = format_date(article.date)
      %a{ :href => article.abspath }= 'Read more'
